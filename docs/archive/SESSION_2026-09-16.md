# Session 16/09/2026 — customer handover and debt reconciliation

## Deployment already completed

- Backend commit `62bb9ed`: allow manual VAT confirmation even when MISA credentials are configured; pending exports remain blocked to prevent duplicate recording.
- Sale-app commit `5168c08`, cherry-picked to production `main` as `b9cf2e2`: remove the MISA export entry point and describe the manual PDF/XML attachment workflow.
- Render backend and Vercel sale-app deployments succeeded. Production UI verified: MISA export button absent; manual confirmation dialog and attachment field present.
- Validation: sale-app build, backend type check, and mocked API checks for authorization, required attachment, amount limit, pending-export guard, and successful manual confirmation. No invoice was issued during verification.

## Production data operations completed with explicit owner authorization

### Customer handover

- Created the requested employee account with member permissions, without company-wide order or VAT issuance privileges.
- Transferred 27 customers from the departing employee and 29 unassigned customers: 56 total.
- Compared all 1,231 organization order records before/after: unchanged, including historical sales attribution. Other customer records were unchanged.
- Verified the supplied initial password against the stored hash; plaintext temporary file removed.

### Customer debt reconciliation

- Reversed two duplicate receipts and reassigned the retained receipt from a nonexistent order to the correct existing order.
- After additional owner confirmation, reversed two older erroneous receipts allocated to cancelled orders, using the existing reversal accounting convention.
- All four receipts remain in the database with reversal timestamps, author attribution, and explanatory notes; no hard deletions.
- Verified final customer balance, active-order debt, and detailed-ledger closing balance are all zero; debit and credit match.
- Order sales values, statuses, and sales attribution remain unchanged. Only the relevant payment allocations and paid/debt balances were corrected.

## Backup and replay policy

Detailed before/after snapshots and verification reports are held locally under the Desktop workspace outputs folders `customer-transfer-2026-09-16` and `greennoly-review-2026-09-16`. They are deliberately excluded from this commit, along with customer identities, credentials, receipt attachments, and database connection strings.

These data operations have already been committed to production. This documentation is not a migration and must not trigger replay on deploy. No schema migration or new application-code change is needed for the data corrections.

## Follow-up context

- MISA callbacks were not re-enabled. Local callback credentials differed from the registered URL, and end-to-end callback verification was not completed before the owner chose the manual workflow.
- Successful accounting synchronization must not be treated as independent proof of electronic-invoice issuance.
